# Copy this file to .env and fill in the values before running docker-compose.

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=dailyknowledge
POSTGRES_PASSWORD=changeme
POSTGRES_DB=dailyknowledge

# ── LLM API (Instructor + OpenRouter) ────────────────────────────────────────
LLM_API_BASE=https://openrouter.ai/api/v1
LLM_API_KEY=sk-...
LLM_MODEL=arcee-ai/trinity-large-preview:free
LLM_RATE_LIMIT=15

# ── Frontend (browser → backend URL) ────────────────────────────────────────
# This is baked into the frontend bundle at build time.
# Use your server's public IP or domain in production.
VITE_API_URL=http://localhost:8000

# ── Backend CORS ──────────────────────────────────────────────────────────────
# Comma-separated list of origins the backend accepts requests from.
CORS_ORIGINS=http://localhost
